import { Typography } from '@mui/material';
import React from 'react';

const News = () => {
    return (
        <div>
            <Typography>All news here</Typography>
        </div>
    );
};

export default News;