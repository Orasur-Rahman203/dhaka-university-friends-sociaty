import { Typography } from '@mui/material';
import React from 'react';

const Events = () => {
    return (
        <div>
            <Typography>All Events are ready</Typography>
        </div>
    );
};

export default Events;