import { Typography } from '@mui/material';
import React from 'react';

const Student = () => {
    return (
        <div>
            <Typography>This is students section</Typography>
        </div>
    );
};

export default Student;